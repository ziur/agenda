<h2>Latest Bookmarks</h2>
<table style="width: 100%">
  <thead>
    <tr>
      <th>Url</th>
    </tr>
  </thead>

  <tbody>
  <% @contacts.each do |contact| %>
    <tr>
      <td><%= link_to contact.name, contact.last_name %></td>
    </tr>
  <% end %>
  </tbody>
</table>
